server:
  servlet:
    context-path: /employee
spring:
  datasource:
    url: jdbc:h2:mem:employee-db
    username: app
    password: 'app'
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    properties:
      hibernate:
        format-sql: true
    show-sql: false