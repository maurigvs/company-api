server:
  servlet:
    context-path: /company
  port: 8280
spring:
  datasource:
    url: jdbc:h2:mem:user-db
    username: app
    password: 'app'
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    properties:
      hibernate:
        format-sql: true
    show-sql: false